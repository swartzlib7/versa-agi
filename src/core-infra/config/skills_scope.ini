# Shipped skill scope overrides (agents.db skills.scope column).
# Default for unlisted skills: all

[scope]
agent_model_management = coa_only
versa_agi_operations_guide = coa_only
utility_models = all
script_tasks = all
