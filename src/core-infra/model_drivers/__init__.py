"""Model drivers package — exact bindings, reusable adapters, artifact writers.

- ``registry`` — exact ``resolve_model_driver`` support truth
- ``libraries`` — shipped reusable DriverAdapter modules
- ``output`` — artifact writers; not ModelDrivers or DriverAdapters
"""
