# Provider logos (GitHub / public)

Published copies of the catalog provider marks. `design/spec/providers/` does not sync to the public repo (`sync_public.sh` excludes `design/`).

Use these paths in README and operator docs. Trademark notes and refresh steps stay on the private spec copy.

README also uses shields for **Google** (`logo=google`), **Vertex** (`logo=googlecloud` — Simple Icons has no Vertex mark; Vertex is a Google Cloud product icon), and **Hugging Face** (`logo=huggingface`).
